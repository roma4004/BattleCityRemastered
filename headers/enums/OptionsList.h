#pragma once

enum class OptionsList : char8_t
{
	WINDOW_WIDTH,
	WINDOW_HEIGHT,
	WINDOW_POS_X,
	WINDOW_POS_Y,
	VSYNC,
	MONITOR_NUMBER,
	SOUND_TOGGLE,
	SOUND_VOLUME,
	SWAP_P1P2_INPUT,

	EndIterator// should be the last one
};
